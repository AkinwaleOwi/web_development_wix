1. Make a copy of the existing quiz - done, if anything just getting the pics together again.

1a. question pattern:

1aa. timer - that stays above as questions go 
1ab. "text - Welcome to Web Cat Trivia by Greene Aeration!"
1ac. "text - Easy difficulty quiz #0!!!"
  
1ad. pic - pic tied to question
1ae. true button - false buttons
1af. previous - next buttons
1ag. submit  - button

2.  **What does the existing JavaScript actually do?**
~~beg of eye test:
2a. timer- ends at the test not per question. 

2b. Chunk/meatus -  everything but the [(prev and next) + submit button ] below and the timer above gets randomzied each quiz.
 -we need to see what the difference between the true button on #8 and #17 is, technically new buttons everytime just exact same color and spot so we cant tell?

 2ba.so the chunk we're randomizing consists of:
  2baa. displaying a pic
  2bab. displaying its related text not voice question
  2bac. true button | | false button = is this where answers are saved as a person flits through questions

2c. previous & next buttons - chunk mover
2d. submit button - submits quiz, then clears the page and displays all the questions/answers + which ones they got right or wrong



~~~end of eye test 
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
# 3\. Learn just enough Velo to get started

 I wouldn't try to learn the entire Velo platform.

 I'd learn these concepts first:

 ### `$w()`

 This lets your code interact with Wix elements.

 For example:

```
$w("#questionText").text = "How many toes does a cat have?";
```

 ### `.onClick()`

 This lets you respond to a button click.

```
$w("#answerButton").onClick(() => {
    // do something
});
```

 ### `.show()` / `.hide()`

 Useful for different quiz states.

```
$w("#resultsSection").hide();
```

 ### `.src`

 Useful for changing the PNG.

```
$w("#questionImage").src = imageUrl;
```

 That's enough to build the first version.

---

